import 'package:flutter/material.dart';

class MyBenefits extends StatelessWidget {
  const MyBenefits({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("My Benefits", style: TextStyle(fontSize: 24)));
  }
}